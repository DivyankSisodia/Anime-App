import 'package:flutter/material.dart';

class AnimeScreens extends StatelessWidget {
  const AnimeScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}